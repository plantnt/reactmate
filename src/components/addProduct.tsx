export default function AddProduct(){
  return(

<select className="bg-furniorange absolute rounded-full hover:bg-orange-500">
  <option value="someOption">+</option>
  <option value="otherOption">Subir un producto</option>
  <option value="otherOption">Ver mis productos</option>
</select>

  )
}